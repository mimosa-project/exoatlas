# AGENTS.md

このドキュメントは、ExoAtlas における「タスク単位の定型作業フロー」を定義する。開発コマンドやアーキテクチャ規約は `.codex` を参照すること（本ファイルはワークフローの定義に専念し、規約を重複して書かない）。

関連ドキュメント:

- 開発コマンド・アーキテクチャ規約: `.codex`
- 要件定義: `docs/requirements.md`
- バックエンド設計: `docs/backend-design.md`
- フロントエンド設計: `docs/frontend-design.md`
- 個別タスクの仕様書: `docs/tasks/<task-slug>.md`（このワークフローで作成する）

## 適用対象

以下のような、仕様や受け入れ基準に影響する変更に適用する。

- 新機能の追加
- API 契約・データ契約の変更
- 既存機能の仕様変更を伴う修正

以下のような軽微な変更は、このフローを省略してよい。ただし省略するかどうか迷う場合はユーザーに確認する。

- typo 修正、コメント・ドキュメントのみの変更
- 依存関係の更新のみで挙動に影響しないもの
- フォーマッタ／リンタの指摘対応のみ

## タスクフロー概要

1. 仕様作成
2. 仕様レビュー（別Agent、クリーンになるまで反復）
3. 単体テスト作成
4. 単体テストレビュー（別Agent、クリーンになるまで反復）
5. 実装
6. 実装レビュー（別Agent、クリーンになるまで反復）

各レビューステップは共通して「[レビューエージェント運用ルール](#レビューエージェント運用ルール)」に従う。

## 各ステップの詳細

### 1. 仕様作成

- 成果物は `docs/tasks/<task-slug>.md` に置く。
- 最低限、以下を含める。
  - 目的・背景
  - スコープ（対象外を明記する）
  - 受け入れ基準（テスト可能な形で書く。例:「〜の場合に〜を返す」）
  - 影響する API／データ契約
  - エッジケース・異常系の扱い
  - 未決事項（あれば）
- `docs/requirements.md` / `docs/backend-design.md` / `docs/frontend-design.md` と整合させる。矛盾する場合は仕様書側で無断上書きせず、矛盾点を明記してユーザーに確認する。

### 2. 仕様レビュー

- 独立したレビューエージェントに、仕様書と関連設計ドキュメントのみを渡す（作成時の議論の経緯は渡さない）。
- レビュー観点:
  - `docs/requirements.md` 等との整合性
  - スコープの過不足（対象外が明記されているか）
  - 受け入れ基準がテスト可能な粒度で書かれているか
  - エッジケース・異常系の考慮漏れ
  - 曖昧な表現の有無
- 指摘があれば仕様を修正し、再度レビューする。Must-fix（上記観点への違反）が無くなるまで繰り返す。Nice-to-have な指摘のみで無限ループさせない。

### 3. 単体テスト作成

- 承認済みの仕様に対してテストを書く。実装より先に書く。
- 新規挙動については、実装が無い（または未修正の）状態でテストが失敗する（red）ことを確認する。何もしなくても通るテストは書き直す。
- 正常系・境界値・異常系／エラーハンドリング・仕様書に明記されたエッジケースを網羅する。
- バックエンド: `tests/` 配下に pytest で追加。フロントエンド: Vitest（未導入の場合は、テスト基盤の導入自体を仕様に含める）。

### 4. 単体テストレビュー

- 独立したレビューエージェントに、仕様書とテストコードのみを渡す（実装はまだ存在しないので渡さない）。
- レビュー観点:
  - 受け入れ基準の各項目に対応するテストがあるか（カバレッジの漏れ）
  - 実装の詳細に依存しすぎていないか（仕様ベースになっているか）
  - 境界値・異常系のテストが十分か
  - fixture／mock の使い方が妥当か
- 仕様レビューと同様、Must-fix が無くなるまで反復する。

### 5. 実装

- レビュー済みのテストと仕様を満たすように実装する。
- 完了前に `.codex` に従い、バックエンドなら `uv run ruff check .` / `uv run pytest` / `uv build`、フロントエンドなら `npm run lint` / `npm run build` 等を実行する。
- テストを実装側の都合で改変しない。テストが誤っていると判明した場合は、ステップ3・4に戻って修正し、実装中にこっそり書き換えない。

### 6. 実装レビュー

- 独立したレビューエージェントに、仕様書・テスト・実装差分（および関連設計ドキュメント）を渡す。
- レビュー観点:
  - 仕様・設計ドキュメントとの整合性
  - テストが実装に合わせて改変されていないか
  - 正確性、エッジケースの扱い
  - セキュリティ（入力検証など）
  - 既存規約（`.codex`）との整合（命名、レイヤー分離など）
- Must-fix が無くなるまで反復する。

## レビューエージェント運用ルール

- **独立性**: レビューのたびに新しいエージェントコンテキストを起動し、作成側の議論の経緯を引き継がせない。渡すのは対象の成果物と関連ドキュメントのみとする。同一コンテキストの延長でレビューさせない。
- **「問題なし」の基準**: ループ継続条件は Must-fix（仕様・受け入れ基準への違反、バグ、テスト不備、規約違反）のみとする。好み・Nice-to-have な指摘は次タスクへのメモに留め、ループは止めない。
- **反復上限**: 各レビューステップは最大3回まで。3回を超えても収束しない場合は自動で続行せず、対立点を整理してユーザーに判断を仰ぐ。

## タスク完了の定義（Definition of Done）

- 上記6ステップが完了している。
- 仕様・テスト・実装の各レビューがクリーン、または反復上限に達した上でユーザー確認済みである。
- `.codex` に定めるチェック（`ruff` / `pytest` / `uv build`、フロントエンドの lint／build 等）が通過している。
- コミットは `.codex` の方針（小さく、実装ステップに沿う）に従っている。
